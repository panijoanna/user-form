import { StyledSection } from "./styled";
import NavigationBar from "../NavigationBar";

const ShowUser = () => {
  return (
    <StyledSection>
      <NavigationBar />
    </StyledSection>
  );
};

export default ShowUser;
